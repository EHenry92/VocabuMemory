
      // console.log('the state', this.state)
      // console.log("the name", evt.target.name)
      // console.log("the value", evt.target.value)
      // this.setState({clicks: this.state.clicks + 1});
      // document.getElementsByClassName('gamePiece').disabled = true;

      // //Can only click 2 cards at once
      // if (this.state.clicks <= 1) {
      //   let card = document.getElementsByClassName('gamePiece')[evt.target.value];
      //   card.classList.add('cardFace');
      //   card.classList.remove('cardBack');
      //   if (this.state.click1 == -1) {
      //    this.setState({click1: evt.target.name}) }
      //    else if (this.click1 !== evt.target.name) {
      //      console.log("dound a match")
      //       setTimeout(() => {
      //         let picks = document.getElementsByClassName('cardFace');
      //         picks[0].classList.add('cardBack')
      //         picks[0].classList.remove('cardFace')
      //         card.classList.add('cardBack')
      //         card.classList.remove('cardFace')
      //        this.setState({clicks: 0})
      //        this.setState({click1: -1})
      //        document.getElementsByClassName('gamePiece').disabled = false;
      //       }, 2500)
      //      }
      //      else {
      //        this.props.postMatch(evt.target.name)
      //        setTimeout(() => {
      //         let picks = document.getElementsByClassName('cardFace');
      //         this.setState({clicks: 0})
      //         document.getElementsByClassName('gamePiece').disabled = false;
      //         picks[0].cdisabled = true;
      //         card.disabled = true;
      //        }, 3000)
      //      }
      // }
